
  # Analytics platform baseline

  This is a code bundle for Analytics platform baseline. The original project is available at https://www.figma.com/design/1GckMU9ZzMuv3YTQPAutOk/Analytics-platform-baseline.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  